version: '3.8'

services:
  fastapi:
    build: .
    container_name: tcq-api
    ports:
      - "6767:6767"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./backend:/app/backend
    command: python -m uvicorn backend.src.main:app --host 0.0.0.0 --port 6767 --reload
